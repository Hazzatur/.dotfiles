from os import path

qtile_path = path.join(path.expanduser('~'), ".config", "qtile")
qtile_scripts_path = path.join(path.expanduser('~'), ".config", "qtile", "scripts")
qtile_settings_path = path.join(qtile_path, "settings")
qtile_images_path = path.join(qtile_path, "images")
